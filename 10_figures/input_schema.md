
# Input schema

Figure scripts consume the nonrestricted derived CSV/JSON files distributed across modules 01-09. Required columns are documented in the corresponding module `input_schema.md` files and in the script-level read statements. The final PDF figures are included as immutable release outputs; TIFF submission files remain in the journal package.
