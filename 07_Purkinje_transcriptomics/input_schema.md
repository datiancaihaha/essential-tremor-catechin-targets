
# Input schema

The count matrix uses approved gene identifiers in rows and donor/sample identifiers in columns. Sample metadata require sample identifier, diagnosis (ET/control), and any covariates used by the selected model. The archived main analysis used a condition-only DESeq2 design for GSE197345, with donors as biological replicates.
